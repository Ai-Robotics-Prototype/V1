# Estun Lua verb reference

_Generated by `scripts/gen_estun_lua_reference.py` from `data/estun_captures/luaenginelib.json` — do not hand-edit._

Source-of-truth path on the controller: `GET /webmodel/cocontrol/luaeditor/luaenginelib.json` (served by the factory UI bundle; the Lua editor uses it to render palette entries and the insertion template).

## Status legend

- **wire-verified** — codegen emits it AND controller-side execution is proven (either a clean run or an error message that identifies the callable).
- **doc-captured** — library entry present with a signature, but no controller-run has proved the exact wire behavior yet.
- **untested** — NOT in `luaenginelib.json`. Appears only in i18n label bundles or syntax-highlighter keyword lists. Do NOT emit without a bench test — the interpreter rejects unknown names with 10012-class errors.

## Counts

| wire-verified | doc-captured | untested | total |
|---:|---:|---:|---:|
| 13 | 155 | 2 | 170 |

## Verbs

| Name | Insertion template | Args | Status | Evidence |
|------|-----|------|--------|----------|
| `ByteWrapper` | `` | — | doc-captured |  |
| `RS485flush` | `$2 = RS485flush($1)` | $2, $1 | doc-captured |  |
| `RS485init` | `$4 = RS485init($1,$2,$3)` | $4, $1, $2, $3 | doc-captured |  |
| `RS485read` | `${var},${res} = RS485read(${rstype},${rlen},${timeout})` | var, res, rstype, rlen, timeout | doc-captured |  |
| `RS485write` | `$3 = RS485write(${rstype},${rsarrayint})` | rstype, rsarrayint | doc-captured |  |
| `acos` | `${var} = math.acos(${fnvar})` | var, fnvar | doc-captured |  |
| `aposToCpos` | `$3 = aposToCpos($1,$2)` | $3, $1, $2 | doc-captured |  |
| `append` | `${var} = ${s1} .. ${s2}` | var, s1, s2 | doc-captured |  |
| `arrayAdd` | `$3 = arrayAdd($1,$2)` | $3, $1, $2 | doc-captured |  |
| `arraySub` | `$3 = arraySub($1,$2)` | $3, $1, $2 | doc-captured |  |
| `arrayToStr` | `${var} = arrayToStr(${vararr},${option})` | var, vararr, option | doc-captured |  |
| `asin` | `${var} = math.asin(${fnvar})` | var, fnvar | doc-captured |  |
| `atan` | `${var} = math.atan(${fnvar})` | var, fnvar | doc-captured |  |
| `atan2` | `${var} = math.atan(${fnvar})` | var, fnvar | doc-captured |  |
| `bitAnd` | `${var} = ${b1} & ${b2}` | var, b1, b2 | doc-captured |  |
| `bitLSH` | `${var} =  ${b1} << ${n}` | var, b1, n | doc-captured |  |
| `bitNot` | `${var} = ~${b1}` | var, b1 | doc-captured |  |
| `bitOr` | `${var} = ${b1} \| ${b2}` | var, b1, b2 | doc-captured |  |
| `bitRSH` | `${var} = ${b1} >> ${n}` | var, b1, n | doc-captured |  |
| `bitXOr` | `${var} = ${b1} ~ ${b2}` | var, b1, b2 | doc-captured |  |
| `break` | `break` | — | doc-captured |  |
| `bwadd` | `` | — | doc-captured |  |
| `bwappend` | `` | — | doc-captured |  |
| `bwcopy` | `` | — | doc-captured |  |
| `bwget` | `` | — | doc-captured |  |
| `bwprintByte` | `` | — | doc-captured |  |
| `bwresize` | `` | — | doc-captured |  |
| `bwset` | `` | — | doc-captured |  |
| `bwsetBigEndian` | `` | — | doc-captured |  |
| `bwsetLittleEndian` | `` | — | doc-captured |  |
| `bwsize` | `` | — | doc-captured |  |
| `byte` | `${var} = table.pack(string.byte(${fnvar}))` | var, fnvar | doc-captured |  |
| `callModule` | `${var} = callModule(${modulename},${submodulename},${argsmodule})` | var, modulename, submodulename, argsmodule | doc-captured |  |
| `char` | `${var} = string.char(${fnvar})` | var, fnvar | doc-captured |  |
| `connectSocketClient` | `${var} = connectSocketClient(${clientname},${timeout})` | var, clientname, timeout | doc-captured |  |
| `coorRel` | `${var} = coorRel(${p1},${offset},${coor})` | var, p1, offset, coor | doc-captured |  |
| `coorTrans` | `${var} = coorTrans($1,$2,${basetypeCT})` | var, basetypeCT | doc-captured |  |
| `cos` | `${var} = math.cos(${fnvar})` | var, fnvar | doc-captured |  |
| `cosh` | `${var} = (math.exp(${fnvar}) + math.exp(-${fnvar})) / 2` | var, fnvar, fnvar | doc-captured |  |
| `cposToApos` | `${var} = cposToApos(${p1},${optional})` | var, p1, optional | doc-captured |  |
| `cposToCpos` | `$4 = cposToCpos($1,$2,$3)` | $4, $1, $2, $3 | doc-captured |  |
| `createMultiWeld` | `` | — | doc-captured |  |
| `createSingleWeldTemplate` | `` | — | doc-captured |  |
| `createSocketClient` | `${var} = createSocketClient($1,$2,$3)` | var | doc-captured |  |
| `createSocketServer` | `${var} = createSocketServer(${servername},${serverport})` | var, servername, serverport | doc-captured |  |
| `createTray` | `createTray(${trayname},${trayconfig},{${traypoints}})` | trayname, trayconfig, traypoints | doc-captured |  |
| `deg` | `${var} = math.deg(${fnvar})` | var, fnvar | doc-captured |  |
| `distance` | `$3 = distance($1,$2)` | $3, $1, $2 | doc-captured |  |
| `else` | `else 
$c
#0end` | — | doc-captured |  |
| `elseif` | `elseif ($1) then
$c
#0end` | $1 | doc-captured |  |
| `equal` | `${var} = ${ke}` | var, ke | doc-captured |  |
| `exp` | `${var} = math.exp(${fnvar})` | var, fnvar | doc-captured |  |
| `find` | `${var} = table.pack(string.find(${fnvar}))` | var, fnvar | doc-captured |  |
| `floor` | `${var} = math.floor(${fnvar})` | var, fnvar | doc-captured |  |
| `fmod` | `${var} = math.fmod(${fnvar})` | var, fnvar | doc-captured |  |
| `for` | `for $1 do
$c
#0end` | $1 | doc-captured |  |
| `format` | `${var} = string.format(${fnvar})` | var, fnvar | doc-captured |  |
| `getAI` | `$2 = getAI($1)` | $2, $1 | doc-captured |  |
| `getAO` | `$2 = getAO($1)` | $2, $1 | doc-captured |  |
| `getCoor` | `${var} = getCoor(${coor})` | var, coor | doc-captured |  |
| `getCurSeam` | `${var} = getCurSeam(${smindex}})` | var, smindex | doc-captured |  |
| `getDI` | `$2 = getDI($1)` | $2, $1 | wire-verified | emitted as _diN = getDI(port) for wait_input; runs without alarm |
| `getDIGroup` | `$3 = getDIGroup($1,$2)` | $3, $1, $2 | doc-captured |  |
| `getDO` | `$2 = getDO($1)` | $2, $1 | doc-captured |  |
| `getDOGroup` | `$3 = getDOGroup($1,$2)` | $3, $1, $2 | doc-captured |  |
| `getExtendArrayData` | `${var} = getExtendArrayData(${geindex},${gedatatype},${gecount})` | var, geindex, gedatatype, gecount | doc-captured |  |
| `getJoint` | `$1 = getJoint()` | $1 | doc-captured |  |
| `getJointExternalTorque` | `$2 = getJointExternalTorque($1)` | $2, $1 | doc-captured |  |
| `getJointTorque` | `$2 = getJointTorque($1)` | $2, $1 | doc-captured |  |
| `getRegisterBool` | `$2 = getRegisterBool($1)` | $2, $1 | doc-captured |  |
| `getRegisterFloat` | `$2 = getRegisterFloat($1)` | $2, $1 | doc-captured |  |
| `getRegisterInt` | `$2 = getRegisterInt($1)` | $2, $1 | doc-captured |  |
| `getTCP` | `$2 = getTCP($1)` | $2, $1 | doc-captured |  |
| `getTool` | `${var} = getTool(${tool})` | var, tool | doc-captured |  |
| `getTrajEnd` | `$2 = getTrajEnd($1)` | $2, $1 | doc-captured |  |
| `getTrajStart` | `$2 = getTrajStart($1)` | $2, $1 | doc-captured |  |
| `getTrayPos` | `${var} = getTrayPos(${trayname},${pnum})` | var, trayname, pnum | doc-captured |  |
| `goto` | `goto $1` | $1 | wire-verified | emitted as goto _prog_start for count=0 continuous loops |
| `gsub` | `${var} = string.gsub(${fnvar})` | var, fnvar | doc-captured |  |
| `if` | `if ($1) then
$c
#0end` | $1 | doc-captured |  |
| `interPos` | `$4 = interPos($1,$2,$3)` | $4, $1, $2, $3 | doc-captured |  |
| `intermittentWelding` | `intermittentWelding(${p1},${p2},${weldlen},${imtstep})` | p1, p2, weldlen, imtstep | doc-captured |  |
| `jointRel` | `${var} = jointRel(${p1},${offset})` | var, p1, offset | doc-captured |  |
| `label` | `::$1::` | $1 | doc-captured |  |
| `len` | `${var} = string.len(${fnvar})` | var, fnvar | doc-captured |  |
| `log` | `${var} = math.log(${fnvar})` | var, fnvar | doc-captured |  |
| `log10` | `${var} = math.log(${fnvar},10)` | var, fnvar | doc-captured |  |
| `lower` | `${var} = string.lower(${fnvar})` | var, fnvar | doc-captured |  |
| `movAS` | `movAS($1,$2,$3,$4,$5,{v=${optional.vv},a=${optional.av},coor=${optional.coor},tool=${op…` | optional.vv, optional.av, optional.coor, optional.tool, optional.swingAmp, optional.swingFreq, optional.useAdaptiveOrientation, optional.search, optional.onpercent | doc-captured |  |
| `movAST` | `movAST($1,$2,$3,${getAST},{v=${optional.vv},a=${optional.av},coor=${optional.coor},tool…` | getAST, optional.vv, optional.av, optional.coor, optional.tool, optional.swingAmp, optional.swingFreq, optional.useAdaptiveOrientation, optional.search, optional.onpercent | doc-captured |  |
| `movC` | `movC($1,$2,{v=${optional.vv},a=${optional.av},b=${optional.b},coor=${optional.coor},too…` | optional.vv, optional.av, optional.b, optional.coor, optional.tool, optional.search, optional.onpercent | doc-captured |  |
| `movCW` | `movCW($1,$2,$3,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional.rb},coo…` | optional.vv, optional.av, optional.b, optional.rb, optional.coor, optional.tool, optional.search, optional.onpercent | doc-captured |  |
| `movCircle` | `movCircle($1,$2,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional.rb},co…` | optional.vv, optional.av, optional.b, optional.rb, optional.coor, optional.tool, optional.circleNum, optional.search, optional.onpercent | doc-captured |  |
| `movJ` | `movJ($1,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional.rb},coor=${opt…` | optional.vv, optional.av, optional.b, optional.rb, optional.coor, optional.tool, optional.search, optional.onpercent | wire-verified | emitted as movJ(pN); controller runs it (bowl program live runs) |
| `movJCoorRel` | `movJCoorRel($1,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional.rb},coo…` | optional.vv, optional.av, optional.b, optional.rb, optional.coor, optional.tool, optional.search, optional.onpercent | wire-verified | emitted as movJCoorRel({cp={0,0,Δz,0,0,0}},{coor=0,tool=0}) fallback path |
| `movJJointRel` | `movJJointRel($1,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional.rb},se…` | optional.vv, optional.av, optional.b, optional.rb, optional.search, optional.onpercent | doc-captured |  |
| `movJToolRel` | `movJToolRel($1,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional.rb},too…` | optional.vv, optional.av, optional.b, optional.rb, optional.tool, optional.search, optional.onpercent | doc-captured |  |
| `movL` | `movL($1,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional.rb},coor=${opt…` | optional.vv, optional.av, optional.b, optional.rb, optional.coor, optional.tool, optional.search, optional.onpercent | wire-verified | emitted as movL(pN); controller runs it (§412 taught contacts + 2026-07-31 STANDARD derived-step columns) |
| `movLCoorRel` | `movLCoorRel($1,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional.rb},coo…` | optional.vv, optional.av, optional.b, optional.rb, optional.coor, optional.tool, optional.search, optional.onpercent | doc-captured |  |
| `movLToolRel` | `movLToolRel($1,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional.rb},too…` | optional.vv, optional.av, optional.b, optional.rb, optional.tool, optional.search, optional.onpercent | doc-captured |  |
| `movLW` | `movLW($1,$2,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional.rb},coor=$…` | optional.vv, optional.av, optional.b, optional.rb, optional.coor, optional.tool, optional.search, optional.onpercent | doc-captured |  |
| `note` | `--$1` | $1 | doc-captured |  |
| `palletizerRun` | `while(true) do
palletizerRun()
end` | — | doc-captured |  |
| `planeTrans` | `$6 = planeTrans($1,$2,$3,$4,$5)` | $6, $1, $2, $3, $4, $5 | doc-captured |  |
| `popUp` | `` | — | doc-captured |  |
| `posInverse` | `$2 = posInverse($1)` | $2, $1 | doc-captured |  |
| `pow` | `${var} = ${s1} ^ ${s2}` | var, s1, s2 | doc-captured |  |
| `print` | `` | — | doc-captured |  |
| `rad` | `${var} = math.rad(${fnvar})` | var, fnvar | doc-captured |  |
| `random` | `${var} = math.random(${fnvar})` | var, fnvar | doc-captured |  |
| `readByteSocketClient` | `${var},${res} = readByteSocketClient(${clientname},${scktype},${isbigendian},${timeout})` | var, res, clientname, scktype, isbigendian, timeout | doc-captured |  |
| `readByteSocketServer` | `${var},${res} = readByteSocketServer(${servername},${scktype},${isbigendian},${timeout})` | var, res, servername, scktype, isbigendian, timeout | doc-captured |  |
| `readCoils` | `${var},${res} = readCoils(${rstype},${raddr},${rlen},${options})` | var, res, rstype, raddr, rlen, options | doc-captured |  |
| `readDiscreteInputs` | `${var},${res} = readDiscreteInputs(${rstype},${raddr},${rlen},${options})` | var, res, rstype, raddr, rlen, options | doc-captured |  |
| `readHoldingRegisters` | `${var},${res} = readHoldingRegisters(${rstype},${raddr},${relen},${options})` | var, res, rstype, raddr, relen, options | doc-captured |  |
| `readInputRegisters` | `${var},${res} = readInputRegisters(${rstype},${raddr},${relen},${options})` | var, res, rstype, raddr, relen, options | doc-captured |  |
| `readSocketClient` | `${var},${res} = readSocketClient(${clientname},${timeout})` | var, res, clientname, timeout | doc-captured |  |
| `readSocketServer` | `${var},${res} = readSocketServer(${servername},${timeout})` | var, res, servername, timeout | doc-captured |  |
| `repeat` | `repeat
$c` | — | doc-captured |  |
| `return` | `return ${ke}` | ke | doc-captured |  |
| `reverse` | `${var} = string.reverse(${fnvar})` | var, fnvar | doc-captured |  |
| `script` | `$1` | $1 | doc-captured |  |
| `searchSuccessed` | `$1 = searchSuccessed()` | $1 | doc-captured |  |
| `setAO` | `setAO($1,$2)` | $1, $2 | wire-verified | emitted as setAO(port,v); part of io_map surface |
| `setAccJ` | `setAccJ(${avd})` | avd | wire-verified | available for future emission; task §2 captured verb (deg/s² modal default) |
| `setAccL` | `setAccL(${avd})` | avd | wire-verified | emitted as setAccL(150) for gentle-descent bracket (task §4); also emitted by RULE 2c descent-split adaptation; modal |
| `setConveyorOffset` | `setConveyorOffset($1,{x=${cx},y=${cy},z=${cz},a=${ca},b=${cb},c=${cc}})` | cx, cy, cz, ca, cb, cc | doc-captured |  |
| `setConveyorTarget` | `${var} = setConveyorTarget(${conveyorId},${conveyorTarget},{type=${cType},id=${cId}})` | var, conveyorId, conveyorTarget, cType, cId | doc-captured |  |
| `setDO` | `setDO($1,$2)` | $1, $2 | wire-verified | emitted as setDO(port,0\|1); operator-observed effect on DO gate |
| `setLeftPallet` | `setLeftPallet(${ptempid},${movParam},${catchParam},${putParam},${boxPoints},${interlaye…` | ptempid, movParam, catchParam, putParam, boxPoints, interlayerPoints | doc-captured |  |
| `setRightPallet` | `setRightPallet(${ptempid},${movParam},${catchParam},${putParam},${boxPoints},${interlay…` | ptempid, movParam, catchParam, putParam, boxPoints, interlayerPoints | doc-captured |  |
| `setSpeedJ` | `setSpeedJ(${vvd})` | vvd | wire-verified | emitted as setSpeedJ(dps) before each joint segment (modal); task §2 captured verb (rated max per-joint [150,150,150,180,180,180] dps from speedLimit page); bench-verify unit at first run |
| `setSpeedL` | `setSpeedL(${vvd})` | vvd | wire-verified | emitted as setSpeedL(mm/s) before each linear segment (modal); task §2 captured verb; controller cartAutoMaxVel=2600 mm/s (robotLimit page); we cap at 1500 mm/s cruise ceiling |
| `sin` | `${var} = math.sin(${fnvar})` | var, fnvar | doc-captured |  |
| `sinh` | `${var} = (math.exp(${fnvar}) - math.exp(-${fnvar})) / 2` | var, fnvar, fnvar | doc-captured |  |
| `sqrt` | `${var} = math.sqrt(${fnvar})` | var, fnvar | doc-captured |  |
| `strToNumberArray` | `${var} = strToNumberArray(${vars},${option})` | var, vars, option | doc-captured |  |
| `strcmp` | `${var} = strcmp(${fnvar})` | var, fnvar | doc-captured |  |
| `sub` | `${var} = string.sub(${fnvar})` | var, fnvar | doc-captured |  |
| `syncConveyorL` | `syncConveyorL($1,$2,$3)` | $1, $2, $3 | doc-captured |  |
| `systemTime` | `$1 = systemTime()` | $1 | wire-verified | reserved: fallback delay idiom if bench proves waitCondition(false,N) short-circuits |
| `tan` | `${var} = math.tan(${fnvar})` | var, fnvar | doc-captured |  |
| `tanh` | `${var} = (math.exp(${fnvar}) - math.exp(-${fnvar})) / (math.exp(${fnvar}) + math.exp(-$…` | var, fnvar, fnvar, fnvar, fnvar | doc-captured |  |
| `tonumber` | `${var} = tonumber(${fnvar})` | var, fnvar | doc-captured |  |
| `toolOffset` | `${var} = toolOffset($1,$2,${basetypeTO})` | var, basetypeTO | doc-captured |  |
| `toolRel` | `${var} = toolRel(${p1},${offset})` | var, p1, offset | doc-captured |  |
| `tostring` | `${var} = tostring(${fnvar})` | var, fnvar | doc-captured |  |
| `trimLeft` | `${var} = string.gsub(${fnvar}, '^%s*(.*)', '%1')` | var, fnvar | doc-captured |  |
| `trimRight` | `${var} = string.gsub(${fnvar}, '(.-)%s*$', '%1')` | var, fnvar | doc-captured |  |
| `until` | `until ($1)` | $1 | doc-captured |  |
| `upper` | `${var} = string.upper(${fnvar})` | var, fnvar | doc-captured |  |
| `userOffset` | `${var} = userOffset($1,$2,${basetypeUO})` | var, basetypeUO | doc-captured |  |
| `waitCondition` | `${var} = waitCondition(${condition},${timeout})` | var, condition, timeout | wire-verified (condition slot: EXPRESSION only) | Legal emission for verify_input: `waitCondition(getDI(N)==<expect>,<timeout_ms>)`. **`waitCondition(false, N)` REJECTED on wire — alarm 10006 ×2** (2026-07-30 08:40 whitebowlpickplace line 11; 2026-07-30 14:08 line 12 after codegen shift). The condition slot must be a runtime-evaluable expression, not a bare boolean/nil literal — despite cowidgets.json saying "false waits for a timeout", firmware v2.3 refuses that shape. Linter's `_KNOWN_BAD_PATTERNS` blocks bare `false`/`true`/`nil` in slot 1 at codegen time. Timeout unit is ms (cowidgets.json). |
| `waitConnectSocketServer` | `${var} = waitConnectSocketServer(${servername},${timeout})` | var, servername, timeout | doc-captured |  |
| `waitConveyorObj` | `${var},${res},${ctype},${cid} = waitConveyorObj(${conveyorId},${timeoutd})` | var, res, ctype, cid, conveyorId, timeoutd | doc-captured |  |
| `weldC` | `weldC($1,$2,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional.rb},coor=$…` | optional.vv, optional.av, optional.b, optional.rb, optional.coor, optional.tool, optional.search, optional.onpercent | doc-captured |  |
| `weldL` | `weldL($1,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional.rb},coor=${op…` | optional.vv, optional.av, optional.b, optional.rb, optional.coor, optional.tool, optional.search, optional.onpercent | doc-captured |  |
| `weldMulti` | `weldMulti(${p1},${p2},{${pwm}},${weldmulti},${lrtrack},${tbtrack},${weldselect})
while(…` | p1, p2, pwm, weldmulti, lrtrack, tbtrack, weldselect | doc-captured |  |
| `weldSingle` | `weldSingle(${weldart},${lrtrack},${tbtrack},${skipweld},${intmtweld})
$c` | weldart, lrtrack, tbtrack, skipweld, intmtweld | doc-captured |  |
| `while` | `while ($1) do
$c
#0end` | $1 | doc-captured |  |
| `writeByteSocketClient` | `${var} = writeByteSocketClient(${clientname},${arrnumber},${scktype},${isbigendian})` | var, clientname, arrnumber, scktype, isbigendian | doc-captured |  |
| `writeByteSocketServer` | `${var} = writeByteSocketServer(${servername},${arrnumber},${scktype},${isbigendian})` | var, servername, arrnumber, scktype, isbigendian | doc-captured |  |
| `writeMultipleCoils` | `$5 = writeMultipleCoils($1,$2,$3,$4)` | $5, $1, $2, $3, $4 | doc-captured |  |
| `writeMultipleRegisters` | `$5 = writeMultipleRegisters($1,$2,$3,$4)` | $5, $1, $2, $3, $4 | doc-captured |  |
| `writeSingleCoil` | `$5 = writeSingleCoil($1,$2,$3,$4)` | $5, $1, $2, $3, $4 | doc-captured |  |
| `writeSingleRegister` | `$5 = writeSingleRegister($1,$2,$3,$4)` | $5, $1, $2, $3, $4 | doc-captured |  |
| `writeSocketClient` | `${var} = writeSocketClient(${clientname},${senddata})` | var, clientname, senddata | doc-captured |  |
| `writeSocketServer` | `${var} = writeSocketServer(${servername},${senddata})` | var, servername, senddata | doc-captured |  |
| `wait` | `wait(<int_ms>)` | ms | WIRE-PROVEN / UNDOCUMENTED | Absent from luaenginelib.json's 168-verb catalogue but resident and executing cleanly on firmware v2.3. Evidence: 2026-07-29 UI screenshot showing `wait(500)` in whitebowlpickplace body + multiple clean bowl-pickplace runs across cycles. The 2026-07-30 alarm-10006 detour that briefly replaced this with `waitCondition(false, N)` and then `systemTime()` loops was a **mis-attribution** — those alarms were on `waitCondition`, not `wait`. Restored as the wait emission and whitelisted in `_WIRE_PROVEN_UNDOCUMENTED`. |
| `setPayload` | `(absent from luaenginelib.json)` | — | untested | i18n label only ("Set the default load"); no callable signature captured in luaenginelib.json; task §6 STOP-CONDITION held — the header comment reports payload_kg but no call is emitted |

## Reference-only for now (in library, not yet emitted)

These verbs are wire-callable per `luaenginelib.json` but the codegen does not emit them this release — capture them here so a future release can add targeted support without re-mining the library.

- `movC` — `movC($1,$2,{v=${optional.vv},a=${optional.av},b=${optional.b},coor=${optional.co`
- `movCircle` — `movCircle($1,$2,{v=${optional.vv},a=${optional.av},b=${optional.b},rb=${optional`
- `movTraj` — (not in library)
- `setMoveRate` — (not in library)
- `setCollisionDetectionSensitivity` — (not in library)
- `getJointExternalTorque` — `$2 = getJointExternalTorque($1)`
- `getJointTorque` — `$2 = getJointTorque($1)`
- `palletizerRun` — `while(true) do
palletizerRun()
end`
- `setConveyorOffset` — `setConveyorOffset($1,{x=${cx},y=${cy},z=${cz},a=${ca},b=${cb},c=${cc}})`
- `setConveyorTarget` — `${var} = setConveyorTarget(${conveyorId},${conveyorTarget},{type=${cType},id=${c`
- `setLeftPallet` — `setLeftPallet(${ptempid},${movParam},${catchParam},${putParam},${boxPoints},${in`
- `setRightPallet` — `setRightPallet(${ptempid},${movParam},${catchParam},${putParam},${boxPoints},${i`
- `syncConveyorL` — `syncConveyorL($1,$2,$3)`
- `waitConveyorObj` — `${var},${res},${ctype},${cid} = waitConveyorObj(${conveyorId},${timeoutd})`

