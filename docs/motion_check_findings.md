# Motion analyzer — findings across on-disk programs

_Generated by `scripts/motion_check_report.py`._

- programs dir: `/opt/cobot/programs`
- parts index:  `/opt/cobot/parts/index.json` (loaded)

## whitebowlpickplace.json

- id: `whitebowlpickplace`
- steps: 13
- program speed_pct: 60
- program motion_profile: joint (default)
- adaptations switch: on (default)
- part_ids bound: []

**5 finding(s), 4 adaptation(s)**

### Findings

- **[ADAPTED] step 0** (Move to home position) — `blend_radius_scaling`  
  blend radius scaled to 8.6mm (profile 12mm capped by 25% × shorter segment 34.4mm)  
- **[WARN] step 0** (Move to home position) — `inconsistent_wrist_orientation`  
  3 taught-point pair(s) differ by more than 20° in wrist axes (max 55.2°) — inconsistent wrist orientations across the program  
  _Suggested action:_ Consider consistent wrist orientation. Use the [Match] tool in the teach drawer once it ships.
- **[ADAPTED] step 1** (Vacuum off (ready)) — `blend_radius_scaling`  
  blend radius scaled to 8.6mm (profile 12mm capped by 25% × shorter segment 34.4mm)  
- **[ADAPTED] step 2** (Approach above pick) — `blend_radius_scaling`  
  blend radius scaled to 8.6mm (profile 12mm capped by 25% × shorter segment 34.4mm)  
- **[ADAPTED] step 7** (Approach above place) — `awkward_wrist_transit`  
  wrist delta 55.2° exceeds 30° threshold — forcing motion_profile=joint for this transit only  
  _Suggested action:_ Consider re-teaching so the two endpoints share wrist orientation. See the J4-saga writeup in PART_2C_ARCHITECTURE.md.

### Adaptations (parameter overrides applied at codegen)

- step 0:  0 move_home       role=home       label='Move to home position'
  - rules: `blend_radius_scaling`
  - blend_radius_override: 8.6 mm
    - shorter adjacent segment 34.4mm × 0.25 = 8.6mm (< profile 12mm)
- step 1:  1 set_io          role=-          label='Vacuum off (ready)'
  - rules: `blend_radius_scaling`
  - blend_radius_override: 8.6 mm
    - shorter adjacent segment 34.4mm × 0.25 = 8.6mm (< profile 12mm)
- step 2:  2 move_linear     role=-          label='Approach above pick'
  - rules: `blend_radius_scaling`
  - blend_radius_override: 8.6 mm
    - shorter adjacent segment 34.4mm × 0.25 = 8.6mm (< profile 12mm)
- step 7:  7 move_linear     role=-          label='Approach above place'
  - rules: `awkward_wrist_transit`
  - force_motion_profile: joint
    - wrist delta 55.2° > 30° — a cartesian movL with a spinning wrist is the worst of both worlds; forcing joint-space transit

### Segment metrics

| step | action | seg len mm | wrist Δ° |
|---:|---|---:|---:|
| 0 | `move_home` (Move to home position) | — | — |
| 1 | `set_io` (Vacuum off (ready)) | — | — |
| 2 | `move_linear` (Approach above pick) | 34.4 | 0.12 |
| 3 | `move_linear` (Pick position — contact) | 100.0 | 0.00 |
| 4 | `set_io` (Engage vacuum) | — | — |
| 5 | `wait` (Wait for vacuum seal) | — | — |
| 6 | `move_linear` (Retreat above pick) | 100.0 | 0.00 |
| 7 | `move_linear` (Approach above place) | 740.5 | 55.15 |
| 8 | `move_linear` (Place position — contact) | 100.0 | 0.00 |
| 9 | `set_io` (Disengage vacuum) | — | — |
| 10 | `move_linear` (Retreat above place) | 100.0 | 0.00 |
| 11 | `move_home` (Return to home) | 746.4 | 55.15 |
| 12 | `loop` (Loop) | — | — |


